name: Meridian Blind 001

on:
  push:
    branches:
      - meridian-blind-001
    paths:
      - 'cleanroom/**'
      - '.github/workflows/meridian-blind.yml'

permissions:
  contents: write
  models: read

jobs:
  blind-evaluation:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout clean-room branch
        uses: actions/checkout@v4
        with:
          ref: meridian-blind-001
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run independent evaluator through GitHub Models
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python cleanroom/run_meridian_blind.py

      - name: Freeze first-pass memo in repository
        run: |
          git config user.name 'ai-q-cleanroom-bot'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add results/MERIDIAN-BLIND-001.md results/MERIDIAN-BLIND-001.metadata.json
          if git diff --cached --quiet; then
            echo 'No new result to commit.'
            exit 0
          fi
          git commit -m 'Freeze MERIDIAN-BLIND-001 first-pass memo [skip ci]'
          git push origin HEAD:meridian-blind-001
